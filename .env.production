# Production environment variables for Vercel deployment

# App settings
APP_NAME=Key Dash Adventure API
APP_VERSION=1.0.0
ENVIRONMENT=production

# API settings (Vercel provides the domain)
API_PREFIX=/api
CORS_ORIGINS=["*"]

# Storage settings (use memory storage in serverless - ephemeral)
PERSIST_TO_DISK=false

# Note: File storage doesn't work in serverless environments
# Data is stored in memory and resets on function cold starts
# For persistent storage, integrate a database (PostgreSQL, MongoDB, etc.)
